<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pig4cloud.pig.common.alert.mapper.SingleAlertMapper">

    <!-- 单条告警结果映射（含 JSON 字段） -->
    <resultMap id="singleAlertResultMap" type="com.pig4cloud.pig.common.core.entity.alerter.SingleAlert">
        <id column="id" property="id"/>
        <result column="fingerprint" property="fingerprint"/>
        <result column="labels" property="labels"
                typeHandler="com.pig4cloud.pig.common.mybatis.handler.JsonMapTypeHandler"/>
        <result column="annotations" property="annotations"
                typeHandler="com.pig4cloud.pig.common.mybatis.handler.JsonMapTypeHandler"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="trigger_times" property="triggerTimes"/>
        <result column="start_at" property="startAt"/>
        <result column="active_at" property="activeAt"/>
        <result column="end_at" property="endAt"/>
        <result column="creator" property="creator"/>
        <result column="modifier" property="modifier"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="gmt_update" property="gmtUpdate"/>
        <result column="fingerprint_id" property="fingerprintId"/>
    </resultMap>

    <!-- 批量更新告警状态 -->
    <update id="updateStatusByIds">
        UPDATE hzb_alert_single SET status = #{status}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
